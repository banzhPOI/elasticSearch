server:
  port: 8080

#  主数据库定义（Primary）
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://poison1994.cn:3306/elasticsearch?serverTimezone=UTC"
    username: "root"
    password: "taobuchu0"
    hikari:
      maximum-pool-size: 20 #连接池同一时间可分配的最大活跃连接数，默认是10

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8